<h2>
  <strong>Comment:</strong>
  <%= @comment.body %>
</h2>

<p>
  <strong>By:</strong>
  <%= link_to User.find(@comment.user_id).name, user_path(User.find(@comment.user_id)) %>
</p>


<button><%= link_to "Edit", edit_comment_path(@comment) %></button>
<%= button_to 'Destroy', @comment, method: :delete,
 :onclick => "return confirm('Are you sure you want to delete this comment?')" %>
<br><br>
<button>
  <%= link_to "Tweet", tweet_path(Tweet.find(@comment.tweet_id)) %>
</button>